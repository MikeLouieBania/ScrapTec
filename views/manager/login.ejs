<!DOCTYPE html>
<html>
<head>
    <title>Manager Login</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500&display=swap" rel="stylesheet">
    <!-- Include Font Awesome icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css">
    <!-- Include Material Symbols Outlined -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
    <style>
        body {
            font-family: 'Poppins', sans-serif;           
            background-color: white;
            height: 50vh;
            margin: 0;
        }
        .red-border {
            color: brown;
            text-align: center;
        }
        
        .login-container {
            max-width: 450px;
            margin: 5% auto;
            padding: 40px;
            background: #002244;
            border-radius: 10px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
            margin-top: 10%;
        }

        .login-container h2 {
            margin-bottom: 15px;
            font-weight: 500;
            color: white;
        }

        .icon-lock {
            font-family: 'Material Symbols Outlined'; /* Set font family for the icon */
            font-size: 50px;
            display: block;
            text-align: center;
            margin-bottom: 20px;
            color: white;
        }

        .form-control {
            border-radius: 5px;
            border: 1px solid #ccc;
            font-size: 14px;
            height: 45px;
            padding-left: 15px;
        }

        .input-group-text {
            border-right: none;
            border-radius: 5px 0 0 5px;
            background-color: #fff;
            color: #aaa;
            font-weight: bold;
        }

        .form-control {
            border-left: none;
            border-radius: 0 5px 5px 0;
        }

        .btn-primary {
            background-color: #5072A7; /* Updated the background color */
            border-color: #5072A7; /* Updated the border color */
            border-radius: 5px;
            padding: 10px 20px;
            font-size: 16px;
            font-weight: bold; /* Made the Login text bold */
            transition: all 0.3s;
            width: 100%; /* Make the button full width */
        }

        .btn-primary:hover {
            background-color: #B0C4DE; /* Darkened the hover background color */
            border-color: #B0C4DE; /* Darkened the hover border color */
            opacity: 0.8;
        }

        .alert-danger {
            border-radius: 5px;
        }

        label { /* Made the Email and Password labels bold */
            color: white;
        }

            /* Responsive Styles */
        @media (max-width: 768px) {
            .login-container {
                padding: 30px;
                margin-top: 20%;
            }

            .login-container h2 {
                font-size: 24px; /* Smaller font size for the heading */
            }

            .icon-lock {
                font-size: 40px; /* Smaller icon size */
            }

            .form-control, .input-group-text, .btn-primary {
                font-size: 14px; /* Smaller font size for form elements */
            }

            .btn-primary {
                padding: 8px 15px; /* Smaller padding for the button */
            }
        }

        @media (max-width: 576px) {
            .login-container {
                padding: 20px;
                margin-top: 30%; /* More margin at the top for very small screens */
            }

            .login-container h2 {
                font-size: 20px; /* Even smaller font size for the heading */
            }

            .icon-lock {
                font-size: 35px; /* Further reduce the icon size */
            }
        }

    </style>
</head>
<body>
    <div class="login-container">
        <span class="icon-lock">lock</span> <!-- Material Symbols Outlined lock icon -->
        <h2 class="text-center">Manager Login</h2>
        <% if (typeof message !== 'undefined') { %>
            <p class="red-border"><%= message %></p>
        <% } %>
        <form action="/manager/login" method="post">
            <div class="form-group">
                <label for="email">Email:</label>
                <div class="input-group">
                    <div class="input-group-prepend">
                        <span class="input-group-text"><i class="fas fa-envelope"></i></span>
                    </div>
                    <input type="text" class="form-control" id="email" name="email" required>
                </div>
            </div>
            <div class="form-group">
                <label for="password">Password:</label>
                <div class="input-group">
                    <div class="input-group-prepend">
                        <span class="input-group-text"><i class="fas fa-lock"></i></span>
                    </div>
                    <input type="password" class="form-control" id="password" name="password" required>
                </div>
            </div>
            <button type="submit" class="btn btn-primary">Login</button>
        </form>
    </div>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.1/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>