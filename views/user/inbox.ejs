<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inbox</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css">
    <style>
            body{
                font-family: 'Poppins', sans-serif;
            }

            /* Navigation Bar Styles */
            .navbar.navbar-expand-lg.navbar-light.bg-light {
                background-color: #219ebc !important;
                padding: 10px 0;
            }
            
            .navbar .nav-link {
                color: white !important;
                font-family: 'Roboto Slab', serif,;
                font-weight: bold;
                font-size: 16px;
                margin-top: 15px;
                display: block;
                padding: 15px;
                text-decoration: none;
                text-transform: uppercase;
                margin: 0 10px;
                position: relative;
                transition: all .5s;
            }
            
            .navbar .nav-link::after {
                content: "";
                position: absolute;
                bottom: 0;
                left: 0;
                right: 0;
                height: 2px;
                background-color: #8ecae6;
                transform: scaleX(0);
                transition: transform 0.5s;
            }
            
            .navbar .nav-link:hover::after {
                transform: scaleX(1);
            }
            
            .navbar-brand {
                color: white !important;
                font-family: 'Roboto Slab', serif,;
                font-weight: bold;
                font-size: 16px;
                margin-top: -0.5px; /* Align with .nav-link */
                margin-left: 15px;
            }
            
            .navbar-light .navbar-toggler-icon {
                background-color: #219ebc;
            }
            
            /* Custom Logout Button Style */
            .btn-outline-success {
                background-color: #002244 !important;
                border: none;
                padding: 10px 20px;
                border-radius: 5px;
                transition: background-color 0.3s;
                margin-right: 20px;
                font-weight: bold;
                color: white !important;
                font-family: 'Roboto Slab', serif,;
            }
            
            .btn-outline-success:hover {
                background-color: #6082B6 !important;
                color: #fff !important;
                text-decoration: none;
            }

            .nav-tabs {
                border-bottom: 2px solid #dee2e6;
            }
            
            .nav-tabs .nav-item.show .nav-link, .nav-tabs .nav-link.active {
                color: #495057;
                background-color: #fff;
                border-color: #dee2e6 #dee2e6 #fff;
            }
            
            .nav-link {
                border: 1px solid transparent;
                border-top-left-radius: 0.25rem;
                border-top-right-radius: 0.25rem;
                transition: border-color 0.3s ease-in-out;
                color: #318CE7;
            }
            
            .nav-link:hover {
                border-color: #adb5bd;
            }
            
            /* Optional: for responsive design */
            @media (max-width: 576px) {
                .nav-tabs .nav-link {
                    margin-bottom: 0.5rem;
                }
            }
                    /* Style for conversation cards */
            .card {
                border-radius: 0.5rem;
                box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
                border: none;
                transition: all 0.3s ease-in-out;
            }

            .card-body {
                padding: 1rem;
            }

            .card-body a {
                text-decoration: none;
            }

            .card-body a:hover {
                background-color: #f8f9fa;
            }

            .card-title {
                font-size: 1.1rem;
                font-weight: bold;
                color: #333; /* Adjust the color as per your theme */
            }

            .card-text {
                font-size: 0.9rem;
                color: #555; /* Adjust the color as per your theme */
            }

            /* Style for badges */
            .badge {
                font-size: 0.8rem;
                padding: 0.25em 0.6em;
            }

            .bg-primary {
                background-color: #007bff !important; /* Primary color */
                color: white;
            }

            .bg-secondary {
                background-color: #6c757d !important; /* Secondary color */
            }

            /* Hover effect for cards */
            .card:hover {
                transform: translateY(-5px);
            }

            /* Responsive adjustments */
            @media (max-width: 768px) {
                .card {
                    margin-bottom: 1rem;
                }
            }

            /* Additional styles (if needed) */
            /* You can add more styles here to match your site's theme */


            .conversation {
                padding: 10px;
                border: 1px solid #ddd;
                margin-bottom: 5px;
                border-radius: 5px;
                transition: background-color 0.3s ease;
            }

            .conversation:hover {
                background-color: #f7f7f7;
                cursor: pointer;
            }

            .unread {
                font-weight: bold;
            }

            #main {
                transition: margin-left .5s;
                padding: 16px;
            }
            /* Responsive adjustments for conversations */
            @media (max-width: 768px) {
                .conversation {
                    padding: 8px;
                    margin-bottom: 3px;
                }
            }
            /* Responsive Navbar */
            @media (max-width: 992px) {
                .navbar-collapse {
                    flex-direction: column;
                    align-items: center;
                    text-align: center;
                }

                .navbar .nav-link,
                .form-inline {
                    width: 100%;
                }

                .form-inline {
                    display: flex;
                    justify-content: center;
                    margin-top: 10px;
                }

                .btn-outline-success {
                    width: auto; /* Prevents the button from expanding */
                }
            }


        /* Target the specific link or text element that has the pseudo-element */
        .nav-link.dropdown-toggle::after, .dropdown-item::after {
            content: none !important; /* Removes the pseudo-element */
        }


    </style>
</head>

<body>
<!-- Navigation Bar -->
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav mr-auto">
            <a class="nav-item nav-link" href="/user/marketplace">HOME</a>
            <a class="nav-item nav-link" href="/user/inbox">Inbox</a>
            <a class="nav-item nav-link" href="/user/buyListing">Buying</a>
            <a class="nav-item nav-link" href="/user/sellListing">Selling</a>
        </div>
        <!-- User Profile Dropdown -->
        <div class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarProfileLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <i class="fa fa-user"></i> <%= userDetails.firstName %> <%= userDetails.lastName %>
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarProfileLink">
                <a class="dropdown-item" href="/user/profile">View Profile</a>
                <!-- Additional dropdown items can be added here -->
            </div>
        </div>
        <form class="form-inline" action="/user/logout" method="POST">
            <button class="btn btn-outline-success" type="submit">LOGOUT</button>
        </form>
    </div>
</nav>


<div class="container mt-5">
    <div class="row">
        <div class="col">
            <ul class="nav nav-tabs justify-content-center" id="inboxTab" role="tablist">
                <li class="nav-item" role="presentation">
                    <a class="nav-link active" id="selling-tab" data-toggle="tab" href="#sellingTab" role="tab" aria-controls="selling" aria-selected="true">
                        <i class="fa fa-tags"></i> Selling
                    </a>
                </li>
                <li class="nav-item" role="presentation">
                    <a class="nav-link" id="buying-tab" data-toggle="tab" href="#buyingTab" role="tab" aria-controls="buying" aria-selected="false">
                        <i class="fa fa-shopping-cart"></i> Buying
                    </a>
                </li>
            </ul>
        </div>
    </div>
    <!-- Tab panes go here -->
</div>



<div class="container mt-5">
    <div class="tab-content mt-3">
        <!-- Selling Tab Content -->
        <div id="sellingTab" class="tab-pane fade show active">
            <% sellingConversations.forEach(conversation => { %>
                <div class="card mb-3">
                    <div class="card-body">
                        <a href="/user/sellConversation/<%= conversation.listing.id %>/<%= conversation.id %>" class="text-decoration-none text-dark">
                            <div class="d-flex justify-content-between align-items-center">
                                <h5 class="card-title mb-0">
                                    <%= conversation.listing.title %>
                                </h5>
                                <span class="badge 
                                    <% if (!conversation.messages[conversation.messages.length - 1].read && conversation.messages[conversation.messages.length - 1].senderId !== userId) { %>bg-primary<% } 
                                    else { %>bg-secondary<% } %>">
                                    <% if (!conversation.messages[conversation.messages.length - 1].read && conversation.messages[conversation.messages.length - 1].senderId !== userId) { %>New<% } 
                                    else { %>Read<% } %>
                                </span>
                            </div>
                            <p class="card-text">
                                <%= conversation.messages[conversation.messages.length - 1].content %>
                            </p>
                        </a>
                    </div>
                </div>
            <% }); %>
        </div>


                 <!-- Buying Tab Content -->
        <div id="buyingTab" class="tab-pane fade">
            <% buyingConversations.forEach(conversation => { %>
                <div class="card mb-3">
                    <div class="card-body">
                        <a href="/user/buyConversation/<%= conversation.listing.id %>" class="text-decoration-none text-dark">
                            <div class="d-flex justify-content-between align-items-center">
                                <h5 class="card-title mb-0">
                                    <%= conversation.listing.title %>
                                </h5>
                                <span class="badge 
                                    <% if (!conversation.messages[conversation.messages.length - 1].read && conversation.messages[conversation.messages.length - 1].senderId !== userId) { %>bg-primary<% } 
                                    else { %>bg-secondary<% } %>">
                                    <% if (!conversation.messages[conversation.messages.length - 1].read && conversation.messages[conversation.messages.length - 1].senderId !== userId) { %>New<% } 
                                    else { %>Read<% } %>
                                </span>
                            </div>
                            <p class="card-text">
                                <%= conversation.messages[conversation.messages.length - 1].content %>
                            </p>
                        </a>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>
</div>

    <!-- Bootstrap JS for tabs and navigation toggling -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

    <script>
        function openNav() {
            document.getElementById("mySidenav").style.width = "250px";
            document.getElementById("main").style.marginLeft = "250px";
        }

        function closeNav() {
            document.getElementById("mySidenav").style.width = "0";
            document.getElementById("main").style.marginLeft = "0";
        }

        function markAsRead(element) {
            element.classList.remove('unread');
        }
    </script>

</body>

</html>