<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= listing.title %></title>
    <!-- Bootstrap CSS for carousel slider and other styles. You can replace it with your own styles if necessary. -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>

<body>

<div class="container mt-5">
    <h2 class="mb-3"><%= listing.title %></h2>
    <p class="mb-4">Price: $<%= listing.price %></p>
    <p>Category: <%= listing.category.name %></p>
    <p>Condition: <%= listing.condition.name %></p>
    <p>Location: <%= listing.location %></p>
    <p>Status: <%= listing.status %></p>
    <p>Seller: <%= listing.user.firstName %> <%= listing.user.lastName %></p>
    <p>Contact Number: <%= listing.user.contactNumber %></p>

    <!-- Image Slider for listing images -->
    <div id="carousel<%= listing.id %>" class="carousel slide mb-4" data-ride="carousel">
        <div class="carousel-inner">
            <% listing.photos.forEach(function(photo, index) { %>
                <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
                    <img class="d-block w-100" style="max-height: 400px;" src="<%= photo.imageUrl %>" alt="<%= listing.title %>">
                </div>
            <% }) %>
        </div>

        <a class="carousel-control-prev" href="#carousel<%= listing.id %>" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carousel<%= listing.id %>" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>

    <!-- Listing description -->
    <div class="listing-description mb-4">
        <h5>Description</h5>
        <p><%= listing.description %></p>
    </div>

    <!-- Send seller a message -->
    <div class="contact-seller">
        <h5>Contact Seller</h5>
        <div class="suggested-messages mb-2">
            <p>Suggested messages:</p>
            <button class="btn btn-sm btn-outline-primary suggest-btn" data-message="Is this item still available?">Is this available?</button>
            <button class="btn btn-sm btn-outline-primary suggest-btn" data-message="Can I get a discount?">Any discount?</button>
            <!-- Add more buttons for other suggested messages -->
        </div>
        
        <form action="/send_message" method="POST">
            <input type="hidden" name="listing_id" value="<%= listing.id %>">
            <textarea name="message" rows="4" class="form-control mb-3" placeholder="Type your message to the seller..."></textarea>
            <button type="submit" class="btn btn-primary">Send Message</button>
        </form>
    </div>

    <!-- You can add more listing details here as needed -->

</div>

<!-- Bootstrap JS and jQuery for carousel functionality -->
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>

</html>
