<!DOCTYPE html>
<html>
<head>
    <title>Advertise in <%= city.name %></title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"/>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Poppins', sans-serif;
          }
         /* Navigation Bar Styles */
         .navbar.navbar-expand-lg.navbar-light.bg-light {
            background-color: #219ebc !important;
            padding: 10px 0;
        }


        .nav-link {
            color: white !important;
            font-family: 'Roboto Slab', serif,;
            font-weight: bold;
            font-size: 16px;
            margin-top: 15px;
        }

        .navbar-brand {
            color: white !important;
            font-family: 'Roboto Slab', serif,;
            font-weight: bold;
            font-size: 16px;
            margin-top: 1px; /* Align with .nav-link */
            margin-left: 15px;
        }

        .navbar-light .navbar-toggler-icon {
            background-color: #219ebc;
        }

        /* Custom Logout Button Style */
        .btn-outline-success {
            background-color: #002244 !important;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            transition: background-color 0.3s;
            margin-right: 20px;
            font-weight: bold;
            color: white !important;
            font-family: 'Roboto Slab', serif,;
        }

        .btn-outline-success:hover {
            background-color: #6082B6 !important;
            color: #fff !important;
            text-decoration: none;
        }

        /* Body */
        body {
            padding-top: 0px;
            margin-bottom: 60px;
        }

        /* Additional Styles for Navigation Links */
        .nav-link {
            display: block;
            padding: 15px;
            text-decoration: none;
            color: white !important;
            font-weight: bold;
            text-transform: uppercase;
            margin: 0 10px;
            position: relative;
            transition: all .5s;
        }


        .nav-link::after {
            content: "";
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            height: 2px;
            background-color: #8ecae6;
            transform: scaleX(0);
            transition: transform 0.5s;
        }

        .nav-link:hover::after {
            transform: scaleX(1);
        }

        

       /* Specific styles for the advertisement form container */
        .col-md-8 {
        background: #f9f9f9;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0,0,0,0.1);
        margin-top: 30px;
        margin-bottom: 30px; /* Added margin-bottom for spacing */
        /* No width and max-width here to allow Bootstrap's grid to control the width */
        }

        /* Heading inside the advertisement form container */
        .col-md-8 h2 {
        font-weight: 600;
        color: #333;
        margin-bottom: 1em;
        }

        /* Paragraph inside the advertisement form container */
        .col-md-8 p.lead {
        color: #555;
        margin-bottom: 1em;
        }

        /* Form styles inside the advertisement form container */
        .col-md-8 .form-group {
        margin-bottom: 0.5em;
        }

        .col-md-8 .form-group label {
        display: block;
        margin-bottom: .5em;
        font-weight: 600;
        color: #555;
        }

        /* Input and select styles inside the advertisement form container */
        .col-md-8 .form-control,
        .col-md-8 .form-control-file {
        border: 1px solid #ccc;
        border-radius: 4px;
        padding: 8px 12px;
        font-size: 14px;
        width: 100%;
        }

        .col-md-8 .form-control:focus,
        .col-md-8 .form-control-file:focus {
        border-color: #007bff;
        box-shadow: inset 0 1px 1px rgba(0, 0, 0, 0.075), 0 0 8px rgba(0, 123, 255, 0.6);
        }

        /* Button styles inside the advertisement form container */
        .col-md-8 .btn-primary {
        display: block;
        width: auto; /* Let the button size according to its content */
        margin: 10px auto 0; /* Center the button with auto margins */
        background-color: #5072A7;
        border-color: #5072A7;
        color: #fff;
        padding: 8px 16px;
        border-radius: 4px;
        cursor: pointer;
        font-size: 14px;
        transition: background-color 0.3s ease;
        }

        .col-md-8 .btn-primary:hover {
        background-color: #6699CC;
        }

        /* Ensure the 'Previous Advertisements' section does not inherit these styles */
        .col-md-4 {
        /* No specific styles here, it will take the default or other specified styles */
        }

            /* Small devices (landscape phones, 576px and up) */
@media (max-width: 576px) { 
    .navbar-brand, .nav-link {
        font-size: 14px;
        margin-top: 0; /* Adjust alignment */
    }

    .btn-outline-success {
        padding: 8px 15px;
        font-size: 14px;
    }

    .col-md-8, .col-md-4 {
        padding: 15px;
        margin-top: 20px;
        margin-bottom: 20px;
    }

    .col-md-8 h2, .col-md-8 p.lead {
        font-size: 16px; /* Adjust font size */
    }

    .nav-link {
        padding: 10px;
        margin: 0 5px;
    }
}

    /* Medium devices (tablets, 768px and up) */
    @media (max-width: 768px) {
    .navbar-brand, .nav-link {
        font-size: 15px;
    }

    .btn-outline-success {
        padding: 9px 18px;
        font-size: 15px;
    }

    .col-md-8, .col-md-4 {
        padding: 18px;
        margin-top: 25px;
        margin-bottom: 25px;
    }

    .col-md-8 h2, .col-md-8 p.lead {
        font-size: 18px;
    }

    .nav-link {
        padding: 12px;
        margin: 0 7px;
    }
    }

    /* Large devices (desktops, 992px and up) */
    @media (min-width: 992px) {
        /* Styles for larger screens can remain as they are */
    }
    
    </style>
</head>
<body>
    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="/organization/dashboard">DASHBOARD</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mr-auto">  
                <li class="nav-item">
                <a class="nav-link" href="/organization/donationsList">Donations</a>
                </li>
                <li class="nav-item">
                <a class="nav-link" href="/organization/pledgeBasket">Pledge Basket</a>
                </li>  
                <li class="nav-item">
                <a class="nav-link" href="/organization/faq">FAQ</a>
                </li>
                <li class="nav-item">
                <a class="nav-link" href="/organization/account">Account</a>
                </li>
            </ul> 
            <form class="form-inline my-2 my-lg-0" method="post" action="/organization/logout">
                <button class="btn btn-outline-success my-2 my-sm-0" type="submit">LOGOUT</button>
            </form>
        </div>
    </nav>
    
    <div class="container mt-4">
        <div class="row">
            <!-- Advertisement Form -->
            <div class="col-md-8">
                <h2>Advertise in <%= city.name %></h2>
                <p class="lead">Reach out to potential users by advertising in this city!</p>
                <form action="/organization/submitAdvertisement" method="post" enctype="multipart/form-data">
                    <!-- Hidden Field -->
                    <input type="hidden" name="cityId" value="<%= city.id %>">
                    
                    <!-- Fields -->
                    <div class="mb-3">
                        <label for="adTitle" class="form-label">Advertisement Title:</label>
                        <input type="text" class="form-control" id="adTitle" name="title" required>
                    </div> 
                    <div class="mb-3">
                        <label for="adLink" class="form-label">Link:</label>
                        <input type="url" class="form-control" id="adLink" name="link" placeholder="URL for your advertisements">
                    </div>
                    <div class="mb-3">
                        <label for="adImage" class="form-label">Advertisement Image:</label>
                        <input type="file" class="form-control-file" id="adImage" name="imageUrl">
                    </div>
                    <div class="mb-3">
                        <label for="adPlan" class="form-label">Advertisement Plan:</label>
                        <select class="form-control" id="adPlan" name="adPlan" required>
                            <option value="basic">Basic Ad (1 week) - 10 points</option>
                            <option value="premium">Premium Ad (2 weeks with featured placement) - 20 points</option>
                        </select>                        
                    </div>
                    <button type="submit" class="btn btn-primary">Submit Advertisement</button>
                </form>
            </div>
   
            <div class="col-md-4">
                <h4>Previous Advertisements:</h4>
                <% if(city.advertisements && city.advertisements.length > 0) { %>
                    <ul class="list-group">
                        <% city.advertisements.slice(0,5).forEach(ad => { %> <!-- Displaying the top 5 latest ads -->
                            <li class="list-group-item">
                                <strong><%= ad.title %></strong><br>
                                <% if(ad.isActive) { %>
                                    <span class="badge badge-success">Active</span>
                                <% } else { %>
                                    <span class="badge badge-secondary">Expired</span>
                                <% } %>
                            </li>
                        <% }) %>
                    </ul>
                <% } else { %>
                    <p>No advertisements yet for this city.</p>
                <% } %>
            </div>
        </div>
        
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
